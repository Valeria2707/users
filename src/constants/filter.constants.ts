import { FilterConfig } from "../types/filter.types";

export const FILTER_CONFIG: FilterConfig[] = [
  { field: "name", placeholder: "Search by Name" },
  { field: "username", placeholder: "Search by Username" },
  { field: "email", placeholder: "Search by Email" },
  { field: "phone", placeholder: "Search by Phone" },
];
